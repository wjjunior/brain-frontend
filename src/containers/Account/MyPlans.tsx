import React from 'react';

import MyPlansComponent from '../../components/Account/MyPlans';

const MyPlansContainer: React.FC = (props) => {
  return <MyPlansComponent {...props} />;
};

export default MyPlansContainer;
