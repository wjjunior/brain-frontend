import React from 'react';

import EditComponent from '../../components/Patients/Edit';

const EditContainer: React.FC = (props) => {
  return <EditComponent {...props} />;
};

export default EditContainer;
