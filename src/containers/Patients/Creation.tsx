import React from 'react';

import CreationComponent from '../../components/Patients/Creation';

const CreationContainer: React.FC = (props) => {
  return <CreationComponent {...props} />;
};

export default CreationContainer;
